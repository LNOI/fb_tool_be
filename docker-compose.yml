services:
  postgres_fb:
    image: postgres:16.3
    container_name: postgres_hydro
    environment:
      POSTGRES_USER: fbtool
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fbtool_db
    ports:
      - "5445:5432"
    volumes:
      - ./postgres_data_fb:/var/lib/postgresql/data